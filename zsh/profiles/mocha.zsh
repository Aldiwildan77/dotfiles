# mocha profile — loaded with COMPANY=mocha
#
# Tracked, so keep it free of anything employer-private: no cluster names, no
# GCP project ids, no internal domains, no tokens. Those belong in
# ~/.zshrc.d/mocha.zsh, which is never tracked and takes precedence over this
# file when both exist.

echo "mocha.zsh sourced!"
